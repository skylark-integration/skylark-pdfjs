define(['./core/worker.js'], function (a) {
    'use strict';
    const pdfjsVersion = PDFJSDev.eval('BUNDLE_VERSION');
    const pdfjsBuild = PDFJSDev.eval('BUNDLE_BUILD');
    return { a.WorkerMessageHandler };
});